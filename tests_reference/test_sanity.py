def test_ci_wiring_smoke() -> None:
    """Гарантирует хотя бы один собранный тест, даже если ни одна ката не сделана.

    Без этого теста pytest на пустом шаблоне видит 0 собранных тестов и
    выходит с кодом 5 ("no tests collected") — красный пайплайн до того,
    как студент вообще начал работу.
    """
    assert True
